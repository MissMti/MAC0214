#include <bits/stdc++.h>
using namespace std;

long long MAX=1000000007;

signed main() {
    //ios::sync_with_stdio(0);
    //cin.tie(NULL);cout.tie(NULL);

    long long a,b,c,erro=1;
    cin>>a>>b;
    if(abs(a-b)==1 || (a==10)&&(b==1) || (a==1)&&(b==10)){
        cout<<"Yes\n";
    }
    else{
        cout<<"No\n";
    }

}
